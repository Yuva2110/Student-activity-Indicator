## This is END to END Machine Learning Project

step by step 

```git init
   git create README.md
   git add README.md

   git commit -m "First commit"
   git status
   git branch -M main
   git remote add origin https:/github.com/username/repository

   git remote -v

  // to rename git remote set-url origin https:/github.com/username/newNameRepo
  // git remote -v
  
  git config --global user.name "username"
  git config --global user.name
  git config --global user.email "email"
  git config --global user.email
  git push -u origin main

  git add .
  git status

  git commit -m "setup"
  git push -u origin main

  <hr>

  ## updating everytime after building
  git add .
  git status
  git commit -m "Logging and exception"
  git push -u origin main
  (venv) C:\Users\yuvak\Desktop\projects\MLProject>git remote set-url origin https://github.com/Yuva2110/mlProject.git

(venv) C:\Users\yuvak\Desktop\projects\MLProject>git push -u origin main
Enumerating objects: 17, done.
Counting objects: 100% (17/17), done.
Delta compression using up to 8 threads
Compressing objects: 100% (14/14), done.
Writing objects: 100% (14/14), 2.22 KiB | 2.22 MiB/s, done.
Total 14 (delta 5), reused 0 (delta 0), pack-reused 0
remote: Resolving deltas: 100% (5/5), completed with 1 local object.
To https://github.com/Yuva2110/mlProject.git
   3c54717..b1ca47b  main -> main
branch 'main' set up to track 'origin/main'.

(venv) C:\Users\yuvak\Desktop\projects\MLProject>